import { RoutePlaceholder } from '../components/RoutePlaceholder';

export function LoginPage() {
  return (
    <RoutePlaceholder
      title="Login"
      description="Placeholder da autenticacao. Sem formulario completo nesta etapa."
    />
  );
}
